import React from "react";

type Props = {};

const Hero = (props: Props) => {
  return (
    <div className="bg-regal-blue h-screen flex justify-center">
      <h1 className="">Hero</h1>
    </div>
  );
};

export default Hero;
